#ifndef ___MACROS_H___
#define ___MACROS_H___
    //Boolean
    #define TRUE 1
    #define FALSE 0

    //Cicles
    #define FORI(n) for(int i = 0; i < n; i++)
    #define FORJ(n) for(int j = 0; j < n; j++)
    #define FOR_I_TO_N(j, n) for(int i = j; i < n; i++)
    #define FOR_J_TO_N(i, n) for(int j = i; j < n; j++)
#endif